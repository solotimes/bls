extends index

block content
  .main
    .form
      .form-header
        ul.tabs
          li.tab.current
            a(href='/customers/#{instance.id}')会员信息
          li.tab
            a(href='#')记录
      include ../flash
      include form
